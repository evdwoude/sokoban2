#ifndef MOVEH
#define MOVEH

void test_object(p_game_data_t p_game_data, uint32_t move, t_mv_action action, t_s_dir search_dir);
void move_object(p_game_data_t p_game_data, uint32_t move, t_mv_action action, t_s_dir search_dir);

#endif /* MOVEH */
